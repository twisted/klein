#!/bin/sh

set -e
set -u

export TOX_SKIP_MISSING_INTERPRETERS="False";

case "${TOXENV}" in
    *-pypy*)
        PYENV_ROOT="${HOME}/.pyenv";
        pyenv="${PYENV_ROOT}/bin/pyenv";
        eval "$("${pyenv}" init -)";
        ;;
esac;

exec "$@";
